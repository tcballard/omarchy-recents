#!/usr/bin/env bash
set -euo pipefail
cd -- "$(dirname -- "$0")/.."
node --test tests/model.test.js
python3 -m unittest discover -s tests -p 'test_*.py'
python3 scripts/validate_manifest.py .
if command -v shellcheck >/dev/null; then shellcheck bin/recents-scan bin/recents-act tests/run; fi
